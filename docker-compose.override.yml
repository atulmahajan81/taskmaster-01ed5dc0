version: '3.8'

services:
  api:
    volumes:
      - ./backend:/app
    environment:
      - DEBUG=True

  frontend:
    volumes:
      - ./frontend:/app
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://localhost
    command: yarn dev